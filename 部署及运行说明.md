# Shenzhen Nexus 部署及运行说明

本文档基于当前仓库实际脚本与目录编写，适用于本地开发、测试、前端发布与回滚。

## 1. 环境要求

- Node.js 20+
- pnpm 9+
- Bash 环境（Windows 可使用 Git Bash）
- （部署到 GCS/CDN 时）`gsutil` 已安装并完成认证

## 2. 项目结构（运行相关）

- `traveler-web`：游客端前端
- `apps/merchant-console`：商户后台
- `apps/ops-console`：运营后台
- `services/gateway`：后端网关（Express + TS）
- `infra/scripts/deploy_frontend.sh`：前端发布脚本
- `infra/scripts/rollback_frontend.sh`：前端回滚脚本
- `infra/deployment/cloudrun-gateway.yaml`：Cloud Run 部署清单

## 3. 环境变量配置

### 3.1 根目录 `.env`

从根目录 `.env.example` 复制并按环境填写：

```bash
cp .env.example .env
```

重点变量：

- 基础：`APP_ENV`、`API_BASE_URL`
- Gateway：`PORT`、`STRIPE_SECRET_KEY`、`STRIPE_WEBHOOK_SECRET`、`JWT_SECRET`、`PASS_HMAC_SECRET`、`AI_API_KEY`
- 前端：`VITE_API_BASE_URL`、`VITE_STRIPE_PUBLISHABLE_KEY`、`VITE_SENTRY_DSN`
- 部署：`STORAGE_BUCKET`、`CDN_BASE_URL`、`RELEASE_TAG`、`ROLLBACK_TO`
- 监控阈值：`METRICS_WARN_MS`、`METRICS_SLOW_MS`

### 3.2 `traveler-web/.env`（如需单独前端环境）

可从 `traveler-web/.env.example` 复制：

```bash
cp traveler-web/.env.example traveler-web/.env
```

## 4. 安装依赖

在项目根目录执行：

```bash
pnpm install
```

## 5. 本地运行

在根目录分别开 4 个终端：

```bash
pnpm dev:api
pnpm dev:web
pnpm dev:merchant
pnpm dev:ops
```

默认地址：

- gateway: `http://localhost:3001`
- traveler-web: Vite 默认端口（通常 `5173`）
- merchant-console / ops-console: 各自 Vite 端口

## 6. 构建与测试

### 6.1 全量构建

```bash
pnpm build
```

### 6.2 常用测试

```bash
pnpm test
pnpm test:e2e
```

## 7. 前端发布（GCS/CDN）

发布脚本会将 `traveler-web/dist` 同步到：

- `releases/<RELEASE_TAG>`
- `releases/latest`

并在 `infra/deployment/frontend-release.json` 写入发布清单。

### 7.1 先构建前端

```bash
pnpm --filter traveler-web build
```

### 7.2 Dry-run（推荐先跑）

```bash
STORAGE_BUCKET=gs://your-bucket \
CDN_BASE_URL=https://cdn.example.com \
RELEASE_TAG=20260426-120000 \
pnpm deploy:frontend -- --dry-run
```

### 7.3 正式发布

```bash
STORAGE_BUCKET=gs://your-bucket \
CDN_BASE_URL=https://cdn.example.com \
RELEASE_TAG=20260426-120000 \
pnpm deploy:frontend
```

## 8. 前端回滚

回滚脚本会把指定版本 `releases/<ROLLBACK_TO>` 覆盖到 `releases/latest`。

### 8.1 Dry-run

```bash
STORAGE_BUCKET=gs://your-bucket \
ROLLBACK_TO=20260426-120000 \
pnpm rollback:frontend -- --dry-run
```

### 8.2 正式回滚

```bash
STORAGE_BUCKET=gs://your-bucket \
ROLLBACK_TO=20260426-120000 \
pnpm rollback:frontend
```

## 9. Gateway 部署（Cloud Run）

当前仓库提供清单：`infra/deployment/cloudrun-gateway.yaml`。

部署前请先将镜像地址中的 `PROJECT_ID` 替换为实际项目，并确保 Secret 已存在：

- `STRIPE_SECRET_KEY`
- `STRIPE_WEBHOOK_SECRET`
- `PASS_HMAC_SECRET`
- `AI_API_KEY`

参考命令：

```bash
gcloud run services replace infra/deployment/cloudrun-gateway.yaml --region=<REGION>
```

## 10. 数据层说明（当前策略）

- 当前 gateway 运行态使用 JSON 持久化：`services/gateway/src/db/client.ts` + `services/gateway/data.json`
- PostgreSQL 为后续迁移目标，不在当前阶段强制启用

## 11. 常见问题

### 11.1 `dist` 不存在导致发布失败

先执行：

```bash
pnpm --filter traveler-web build
```

### 11.2 发布脚本提示缺少 `STORAGE_BUCKET`

请在命令前显式传入，或写入 `.env` 后在当前 shell 导出。

### 11.3 E2E 失败

先确认 `gateway` 与 `traveler-web` 可正常启动，再执行：

```bash
pnpm test:e2e
```
