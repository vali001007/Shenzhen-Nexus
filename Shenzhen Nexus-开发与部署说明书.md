## Shenzhen Nexus 开发与部署说明书（APEC 专项升级版）

### 1. 文档目的与产品定位

本说明书用于指导 `Shenzhen Nexus` 从当前的单文件前端原型，升级为一款面向 **2026 APEC 入境国际游客** 的深圳本地智能服务平台，并同时具备 **本地文旅/餐饮商家引流** 与 **高端线下服务交易闭环** 的生产化能力。

相较于旧版说明书，本版在保留现有原型结构分析的基础上，进一步吸收了以下两类关键结论：

- **APEC 场景优化结论**：用户以国际商务人士、代表团成员、随行家属和高净值访客为主，需求集中在翻译、短时深度游、商旅衔接、支付适配、旅拍与尊享服务。
- **竞品分析结论**：本产品不应与 `Trip.com`、大众点评、高德地图在标准库存和基础导航层正面竞争，而应建立在 **深圳本地私有数据 + AI 多模态交互 + 高净值 O2O 履约网络** 之上的差异化能力。

因此，`Shenzhen Nexus` 的目标定位不再是“信息型导览网页”，而是：

- **对 C 端国际游客**：提供语言、行程、支付、交通、文化理解和尊享服务的一站式城市伴侣。
- **对 B 端商户**：提供面向入境客群的精准曝光、预约转化与到店核销能力。
- **对平台自身**：沉淀深圳本地化消费与行为数据，构建持续迭代的推荐与商业化基础设施。

---

### 2. 系统架构和技术栈说明

### 2.1 当前原型现状

当前项目核心文件为 `Shenzhen Nexus.HTML`，属于 **单页静态高保真原型**，主要技术特点如下：

- **表现层**：HTML5 + Tailwind CSS CDN + 自定义 CSS
- **交互层**：原生 JavaScript
- **状态方式**：页面内全局变量维护，如 `currentLang`、`currentTabId`
- **AI 接入**：浏览器端 `fetch` 直接请求 Gemini 接口
- **资源组织**：第三方字体、图标、样式均通过 CDN 引入
- **渲染方式**：DOM 注入 + 模板字符串拼接 + Overlay 模块切换

该架构适合产品演示和需求确认，但无法支撑以下生产要求：

- APEC 期间高并发访问
- 外部支付与服务交易闭环
- 商户入驻和标签化运营
- 用户身份、订单、核销与分账
- API 密钥安全、鉴权、限流与日志审计
- 自动化测试、灰度发布和故障回滚

### 2.2 升级后的目标架构

建议将系统升级为 **前后端分离 + Serverless/BFF + 商户与履约中台** 架构，推荐划分为 6 层：

#### 2.2.1 客户端层

面向不同使用场景提供多端入口：

- **移动 Web / PWA**：面向海外用户的即开即用入口
- **微信小程序**：面向已进入中国生态的用户，便于连接支付、核销和本地服务
- **运营后台 Web**：面向平台运营、商户运营、服务人员管理

#### 2.2.2 前端体验层

承载游客端 UI、商户导购、订单流转与本地化交互：

- 首页探索与 APEC 专属模式
- AI 问答与语音同传
- AI 微行程规划
- AR 菜单/招牌/地标解码
- APEC 友好商户卡片与榜单
- Concierge+ 尊享服务预约
- Pass 卡与到店核销入口

#### 2.2.3 BFF / API 网关层

统一接收前端请求，对外提供稳定、安全、可审计的 API：

- `POST /api/concierge`
- `POST /api/translate/realtime`
- `POST /api/itinerary/generate`
- `POST /api/vision/decode`
- `POST /api/merchant/search`
- `POST /api/service/order`
- `POST /api/pass/redeem`
- `POST /api/payment/create`

该层负责：

- API 鉴权与防刷
- 统一调用 AI 服务
- 敏感词与内容安全检查
- 响应结构标准化
- 请求日志和行为埋点
- 熔断、重试、降级

#### 2.2.4 业务服务层

建议将核心业务拆为以下服务：

- **AI 服务**：问答、翻译、行程、视觉理解、TTS
- **内容服务**：景点、榜单、场馆、城市须知、会务信息
- **商户服务**：商户入驻、标签、推荐位、券码、核销
- **履约服务**：导游、翻译、旅拍、接送、预约排班
- **交易服务**：订单、支付、退款、佣金结算
- **用户服务**：游客偏好、收藏、历史行程、授权信息

#### 2.2.5 数据层

建议采用结构化数据库 + 对象存储 + 缓存组合：

- **关系型数据库**：用户、商户、订单、服务人员、核销记录、支付流水
- **对象存储**：景点图片、商户图、旅拍作品、AR 识别上传图片
- **缓存层**：热点商圈、榜单、会场周边推荐、FAQ、Prompt 模板
- **日志仓库**：行为埋点、AI 请求日志、支付与核销日志、错误事件

#### 2.2.6 运维与治理层

- CDN
- 域名与 HTTPS
- 监控、告警、审计
- CI/CD 发布流水线
- 灰度、回滚与容量规划
- 数据脱敏与隐私治理

### 2.3 推荐技术栈

### 2.3.1 前端技术栈

建议采用：

- **React + Vite + TypeScript** 或 **Vue 3 + Vite + TypeScript**
- **Tailwind CSS**：保留当前视觉风格的同时增强工程化能力
- **状态管理**：`Zustand` / `Pinia` / `Redux Toolkit` 任选一种
- **国际化方案**：`react-i18next` / `vue-i18n`
- **表单与校验**：`zod` / `yup`
- **数据请求**：`fetch` 封装或 `axios`
- **PWA 支持**：离线资源缓存、桌面快捷入口、弱网增强

### 2.3.2 后端技术栈

建议优先采用 **Node.js + TypeScript** 的 Serverless / BFF 架构，例如：

- **CloudBase 云函数 / CloudRun** 或其他等价 Serverless 平台
- **Express / Fastify / NestJS** 作为 API 服务框架
- **队列 / 异步任务**：用于旅拍分配、消息通知、核销同步、报表生成

### 2.3.3 AI 与多模态能力

建议服务端统一代理调用模型：

- **文本问答模型**：用于深圳问答、礼仪说明、应急建议
- **结构化输出模型**：用于微行程生成、商户推荐、服务分发
- **视觉模型**：用于菜单、路牌、商品、地标识别与解释
- **TTS / 语音能力**：用于应急短语播报与翻译结果播放
- **实时语音流**：用于“对讲机模式”同传

### 2.3.4 商业化与生态集成能力

建议预留以下集成：

- 国际支付：`Stripe`、`PayPal`
- 中国境内支付引导：微信支付、支付宝 TourCard
- 本地服务连接：滴滴、高端车队、旅拍预约系统、票务平台
- 地图与定位：高德/腾讯地图国际适配方案
- 通知：短信、邮件、WhatsApp/企业微信等服务

### 2.4 关键架构原则

结合竞品结论，系统设计必须坚持以下原则：

- **不做大而全 OTA**，聚焦“行中服务”和“深圳本地深度体验”
- **不与本地地图正面竞争**，重点做语言桥接、场景引导和服务转化
- **所有 AI 能力必须绑定本地私有知识库**，降低泛化幻觉
- **所有收费服务必须可追踪、可履约、可结算**
- **所有海外用户数据必须遵循隐私合规要求**

---

### 3. 开发环境配置指南

### 3.1 角色分工建议

建议至少划分以下开发角色：

- **前端工程师**：负责游客端、商户端和运营后台界面
- **后端工程师**：负责 API、订单、支付、商户、服务履约逻辑
- **AI 工程师 / Prompt 工程师**：负责知识库、Prompt、结构化输出、召回策略
- **测试工程师**：负责接口、E2E、兼容性和验收
- **运营与内容团队**：负责商户数据、会务信息、多语言内容和核销配置

### 3.2 基础开发环境

统一建议如下：

- **Node.js**：`18.x` 或以上
- **包管理器**：`pnpm` 优先，也可使用 `npm`
- **Git**：版本管理
- **编辑器**：VS Code
- **浏览器**：Chrome、Edge、Safari 最新版本
- **数据库客户端**：用于查看开发数据
- **Postman / Apifox**：用于接口调试

### 3.3 建议目录结构

```text
shenzhen-nexus/
├─ apps/
│  ├─ traveler-web/
│  ├─ merchant-console/
│  └─ ops-console/
├─ services/
│  ├─ gateway/
│  ├─ ai-service/
│  ├─ itinerary-service/
│  ├─ merchant-service/
│  ├─ order-service/
│  └─ payment-service/
├─ packages/
│  ├─ ui/
│  ├─ i18n/
│  ├─ config/
│  └─ utils/
├─ infra/
│  ├─ scripts/
│  ├─ ci/
│  └─ deployment/
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  └─ e2e/
├─ docs/
├─ .env.example
├─ package.json
└─ README.md
```

### 3.4 环境变量建议

至少配置以下变量：

- `APP_ENV`
- `API_BASE_URL`
- `AI_PROVIDER`
- `GEMINI_API_KEY`
- `PAYMENT_PROVIDER`
- `STRIPE_SECRET_KEY`
- `WEBHOOK_SIGNING_SECRET`
- `DB_URL`
- `STORAGE_BUCKET`
- `CDN_BASE_URL`
- `LOG_LEVEL`
- `RATE_LIMIT_QPS`

### 3.5 本地启动建议

#### 3.5.1 前端

```powershell
pnpm install
pnpm --filter traveler-web dev
```

#### 3.5.2 后端 API

```powershell
pnpm --filter gateway dev
```

#### 3.5.3 测试

```powershell
pnpm test
pnpm e2e
```

### 3.6 数据与内容初始化

开发环境需准备以下初始化数据：

- APEC 会场与周边商圈数据
- APEC 友好商户样本数据
- 导游/翻译/旅拍服务人员样本数据
- 中英双语城市知识库
- 景点、餐饮、夜游和商务休闲内容库
- 支付、核销和订单的 Mock 数据

### 3.7 原型迁移注意事项

从 `Shenzhen Nexus.HTML` 迁移时，需优先处理以下问题：

- 将模板字符串型模块拆为组件
- 将 `i18nDict` 拆为独立资源文件
- 将 `callGeminiText()`、`callGeminiTTS()` 等迁移到服务端代理
- 补齐缺失的 `callGeminiImage()` 能力
- 将“保存路线”“预约导游”等演示按钮接入真实后端数据流

---

### 4. 核心功能模块的详细实现说明

### 4.1 首页与 APEC 专属模式

首页不应只承载静态入口，而应成为“场景化服务中枢”。建议首页按以下区域重构：

- **APEC Banner**：展示会议时间、会场区域、天气、交通提醒
- **会场周边推荐**：根据用户位置或酒店位置推荐 15-30 分钟可达地点
- **快捷操作区**：翻译、微行程、用车、支付、网络、紧急帮助
- **尊享服务区**：旅拍、导游、翻译、商务休闲、夜游专线
- **APEC 友好商户专区**：优先展示可英语服务、可刷国际卡、可预约的商户

#### 实现要点

- 使用定位或用户手动选择“会场/酒店/商圈”作为上下文
- 支持基于时间窗口的推荐，如“2 小时空档”“晚宴后夜游”
- 首页卡片使用推荐引擎动态排序，而不是全部静态写死

### 4.2 AI Concierge 问答与实时翻译

该模块是对抗通用大模型的核心能力之一，必须建立在深圳本地知识库之上。

#### 目标能力

- 深圳礼仪、交通、支付、会场周边、餐厅、夜游、紧急事项问答
- 中英为核心，逐步扩展日语、法语、西班牙语、俄语
- 文本问答 + 语音播报 + 实时同传

#### 实现方案

- 前端提供文本输入、语音按钮、翻译对讲模式
- 后端先检索本地知识库，再拼接 Prompt 调用模型
- 对输出结果做结构化包装：
  - `answer`
  - `tips[]`
  - `relatedServices[]`
  - `recommendedMerchants[]`

#### 优化重点

- 禁止模型仅凭泛知识回答本地商户营业状态
- 引入 FAQ 缓存与热点问题命中
- 记录失败问题与低质量回答，持续更新知识库

### 4.3 AI 微行程规划模块

结合 APEC 用户的时间碎片化特征，行程模块需从“半天游”升级为“微行程引擎”。

#### 输入要素

- 当前时间和剩余空闲时长
- 出发位置（会场/酒店/商圈）
- 兴趣标签（科技、文化、美食、夜游、购物、商务放松）
- 是否需要翻译、导游、用车或旅拍
- 消费预算与同行人数

#### 输出结构

- `title`
- `duration`
- `routeSummary`
- `stops[]`
- `transportAdvice`
- `bookableServices[]`
- `merchantOffers[]`

#### 差异化要求

- 主打深圳本地碎片化、非标化体验
- 避免与 `Trip.com` 的标准门票/酒店库存重叠
- 每一条行程都允许一键升级为付费履约服务

### 4.4 AR 解码与城市理解模块

该模块是本产品最具辨识度的差异化入口之一。

#### 目标场景

- 菜单翻译
- 路牌/指示牌识别
- 科技产品/无人设备识别
- 景点与建筑解说
- 商户海报或活动信息识别

#### 输入/输出建议

- **输入**：图片、语言、识别场景类型
- **输出**：
  - `term`
  - `translation`
  - `pinyin`
  - `category`
  - `context`
  - `actionableTips[]`

#### 当前缺口

原型虽已完成上传与 UI，但 `callGeminiImage()` 缺失，需在重构时优先补齐。

#### 生产要求

- 增加图片大小限制和超时机制
- 对识别内容进行安全过滤
- 对模糊识别结果增加置信度和用户二次确认

### 4.5 APEC 友好商户系统

这是 B 端引流能力的核心，不只是列表展示，而是平台商业化能力的基础。

#### 商户侧能力

- 商户入驻与资料审核
- 标签配置：`APEC-Ready`、`English Menu`、`Foreign Cards Accepted`
- 套餐、优惠券、核销码、预约时段管理
- 素材管理：封面图、菜单图、双语说明、接待能力说明

#### 游客侧能力

- AI 推荐商户
- 列表筛选与地图查看
- 预约/导航/收藏/分享
- 到店出示 Pass 或优惠码核销

#### 推荐逻辑

结合以下信号排序：

- 地理距离
- 会场可达性
- 当前营业状态
- 语言服务能力
- 国际卡支付支持度
- 商业投放等级
- 历史转化表现

### 4.6 Concierge+ 尊享服务平台

这是区别于 OTA 和点评类产品的高价值模块。

#### 服务品类

- 多语种陪同翻译
- 本地导游
- 高端旅拍
- 商务车接送
- 夜游专线
- 快速陪同购物/电子市场导购

#### 业务流程

1. 用户从首页、行程页或商户页发起预约
2. 系统根据语言、时间、地点、预算匹配服务人员
3. 用户确认价格并支付定金或全款
4. 平台分配服务人员并发送通知
5. 服务完成后触发评价、结算与佣金分账

#### 技术要求

- 订单状态机
- 服务人员排班管理
- 售后与退款机制
- 平台抽佣和对账报表

### 4.7 支付与交易模块

该模块是 APEC 场景成败关键之一。

#### 核心目标

- 支持国际用户直接支付平台内服务
- 支持商户券包、服务订单、旅拍、导游预约支付
- 支持退款、改约、失败补单和支付结果回调

#### 推荐方案

- 平台内高客单价服务优先接 `Stripe` / `PayPal`
- 对境内消费场景提供微信支付/支付宝绑定引导
- 所有支付必须经过服务端签名和回调验证

### 4.8 Pass 卡与核销系统

该模块连接游客、商户和平台结算。

#### 功能要求

- 发放 APEC 专属数字 Pass
- 按活动、商圈、商户生成不同券权益
- 到店扫码核销
- 记录核销时间、门店、订单来源和佣金归属

#### 技术要点

- 二维码动态生成
- 核销码防重复使用
- 商户端核销权限控制
- 核销日志可追踪

### 4.9 用户体系与画像系统

虽然入境游客停留时间短，但用户体系仍然必要。

#### 最低能力

- 游客匿名访问
- 邮箱登录或魔法链接登录
- 偏好记录与收藏
- 历史问答和历史行程

#### 进一步增强

- APEC 身份标签识别
- 会场/酒店偏好记忆
- 高净值用户识别与个性化服务推荐

### 4.10 内容与知识库系统

为避免 AI 幻觉，必须建立专属内容底座。

#### 内容范围

- 深圳景点与科技地标
- 会展和会场周边信息
- 商户基础资料与营业状态
- 城市礼仪、支付、交通、网络说明
- 应急信息与官方资源

#### 维护方式

- 内容后台人工录入 + 审核发布
- 支持双语或多语版本
- 重点内容可人工置顶
- 会务和活动信息支持定时上下线

---

### 5. 测试方案和验收标准

### 5.1 测试目标

本项目测试不仅要验证页面功能，还要验证：

- AI 输出质量
- 商户推荐准确性
- 订单与支付闭环可靠性
- APEC 场景下的多语言可用性
- 弱网和跨境访问稳定性

### 5.2 测试分层

#### 5.2.1 单元测试

覆盖：

- i18n 词典映射
- Prompt 构建与输出解析
- 推荐排序规则
- 订单状态机
- Pass 核销校验逻辑
- 金额和佣金计算逻辑

#### 5.2.2 集成测试

覆盖：

- 前端到网关 API 联调
- AI 服务结构化返回验证
- 支付下单与回调联调
- 商户预约与服务分配流程
- 核销与结算记录同步

#### 5.2.3 端到端测试

建议使用 `Playwright` 覆盖以下关键链路：

- 切换语言并完成 APEC 问答
- 生成 2 小时微行程并跳转到预约导游
- 上传图片完成 AR 解码
- 搜索 APEC 友好商户并领取优惠
- 创建旅拍订单并进入支付页
- 商户端完成核销操作

#### 5.2.4 非功能测试

- 跨境弱网测试
- 峰值并发测试
- CDN 资源失败降级测试
- Web 安全测试
- 隐私与权限测试

### 5.3 测试重点场景

#### 场景 1：国际访客临时空档规划

- 输入“我在福田，2 小时后回会场，想喝咖啡并拍几张夜景照”
- 系统需返回可执行微行程，并可预约旅拍或用车

#### 场景 2：菜单即时翻译

- 用户上传菜单图片
- 系统返回翻译、拼音、用餐建议和文化解释

#### 场景 3：Apec 友好商户转化

- 用户筛选“可刷外卡 + 英文菜单”
- 成功查看商户详情、领取权益并完成到店核销

#### 场景 4：实时同传

- 用户与商家分别说不同语言
- 系统在合理时延内完成双向语音翻译

#### 场景 5：服务订单履约

- 用户预约导游或旅拍
- 平台完成派单、支付确认、服务完成与结算

### 5.4 验收标准

#### 产品验收

- 游客端核心链路完整可用
- 至少支持中英双语闭环，无关键页面漏翻
- 微行程、AR 解码、商户推荐、服务预约均可演示完整业务链路
- 重点页面在移动端体验流畅

#### 技术验收

- 不允许前端暴露正式 AI Key 或支付密钥
- 主要接口均有统一错误码与日志追踪
- AI 结构化接口成功率达到目标阈值
- 支付回调、核销、订单状态无数据错乱

#### 业务验收

- APEC 友好商户标签和内容准确
- 至少具备一套真实优惠券或 Pass 核销流程
- 至少具备一类可实际成交的尊享服务流程

---

### 6. 部署流程和运维方案

### 6.1 环境划分

建议至少区分：

- **dev**：开发联调环境
- **staging**：测试与验收环境
- **prod**：生产环境

每套环境需隔离：

- 域名或子域名
- 数据库与存储桶
- API Key 与支付密钥
- 日志与监控指标
- 发布权限与审批流程

### 6.2 前端部署流程

1. 从主干或发布分支构建前端产物
2. 执行代码检查、单元测试和 E2E 冒烟
3. 构建静态资源并上传至静态托管/CDN
4. 发布后校验：
   - 首页加载
   - 国际化资源
   - 图片资源
   - 服务端 API 连通性
5. 灰度验证后全量切流

### 6.3 后端部署流程

1. 配置环境变量与密钥
2. 发布网关、AI 服务、订单服务、支付服务
3. 执行数据库迁移
4. 运行联调与健康检查
5. 执行灰度发布
6. 监控错误率、超时率、支付异常率
7. 达标后切换正式流量

### 6.4 数据部署与初始化

生产环境上线前必须完成：

- APEC 场馆与会场周边内容初始化
- 商户基础数据导入
- 服务人员与排班导入
- 支付通道、Webhook、退款回调配置
- Pass 模板与券包规则配置

### 6.5 运维监控方案

#### 6.5.1 指标监控

重点监控：

- PV / UV
- 首屏加载时间
- API 成功率和响应时长
- AI 请求成功率与成本消耗
- AR 解码成功率
- 微行程生成成功率
- 支付成功率与退款率
- 核销成功率
- 商户转化率

#### 6.5.2 日志与审计

需要记录：

- AI 请求摘要和响应状态
- 订单全生命周期
- 支付回调与退款日志
- 商户核销日志
- 用户关键行为埋点
- 运营配置变更记录

#### 6.5.3 告警策略

至少配置：

- AI 服务超时或异常告警
- 支付失败率突增告警
- 核销接口异常告警
- 前端运行时错误告警
- CDN 资源异常告警
- 流量暴涨和防刷告警

#### 6.5.4 安全与合规

必须落实：

- 服务端保管密钥
- 关键接口鉴权、限流、防重放
- 用户上传图片与文本内容安全检查
- PII 脱敏和日志最小化原则
- 隐私协议、Cookie 同意、数据删除申请通道
- 满足 `GDPR` 与 `PIPL` 基本要求

#### 6.5.5 回滚与灾备

- 前端静态资源版本化回滚
- 后端灰度/蓝绿发布回退
- 数据库变更可回滚或具备备份恢复点
- 支付和订单服务单独健康探针

---

### 7. 后续扩展建议

### 7.1 分阶段实施路线

#### 第一阶段：原型工程化（立即执行）

- 拆分单 HTML 原型
- 补齐 `callGeminiImage()`
- 建立 API 网关与 AI 代理
- 完成中英双语闭环
- 打通基础微行程与商户推荐

#### 第二阶段：APEC 核心能力上线

- 上线 APEC 专属模式
- 上线 APEC 友好商户系统
- 上线 Pass 核销流程
- 上线 Concierge+ 基础预约
- 上线国际支付能力

#### 第三阶段：商业化深化

- 精细化推荐与转化分析
- 商户投放等级与推荐位策略
- 订单抽佣、报表与结算系统
- 高净值用户分层运营

#### 第四阶段：平台化复制

- 从深圳复制到大湾区其他城市
- 输出白标或行业解决方案
- 提供城市级文旅数据分析服务

### 7.2 需要优先解决的技术债

- 单文件结构难以维护
- AI 能力仍有浏览器直连风险
- AR 解码缺失关键实现
- 商户和订单体系尚未落地
- 多语言仅部分完成
- 没有正式测试与发布流水线

### 7.3 核心战略结论

从产品策略上看，`Shenzhen Nexus` 的开发方向必须围绕以下三点展开：

- **做“入境游客在深圳的超级助手”**，而不是做通用旅游平台
- **做“本地服务桥梁”**，而不是做底层地图或库存平台
- **做“高价值履约网络 + 本地私有数据”**，而不是只靠静态内容和单次 AI 问答

只要开发和部署遵循本说明书所定义的架构与实施路径，团队即可在保留现有原型优势的基础上，把 `Shenzhen Nexus` 演进为一款具备 APEC 场景适配能力、商户引流能力和持续商业化空间的正式产品。