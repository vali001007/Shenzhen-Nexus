## Shenzhen Nexus 技术实施任务清单（基于 V1.1 PRD 融合版）

### 1. 使用说明

本清单用于将 `Shenzhen Nexus` 从原型推进为可上线、可交易、可运营的产品系统，并与最新版 PRD（Top Spots × APEC 友好商户融合）保持一致。

任务优先级定义：

- **P0**：上线阻塞项，不完成无法进入发布
- **P1**：首版核心能力，建议首版本完成
- **P2**：增强与优化能力，可在后续迭代完成

实施原则：

- 以“**榜单 → 融合详情 → CTA → 到店核销/履约**”为主闭环优先实现
- 前端先完成可用链路，再补齐服务端能力与后台治理
- 所有关键行为必须有埋点与转化归因

---

### 2. 里程碑与范围映射

### 2.1 M0（已验证）

- 已完成 `Top Spots × 商户融合` 交互原型验证（单文件）

### 2.2 MVP（必须）

- AI 问答、微行程、AR 解码
- Top Spots 融合榜单（分类筛选 + 动态卡片）
- Spot 融合详情层（场景匹配、消费路径、附近商户）

### 2.3 V1（交易闭环）

- 商户详情、服务预约、支付、Pass 领取与核销
- 融合链路关键埋点与转化归因

### 2.4 V1.1（运营优化）

- 后台管理、推荐策略优化、用户体系增强
- Spot-商户智能匹配策略上线

---

### 3. 前端工程化与体验任务

### 3.1 工程初始化与架构拆分

- **P0**：将 `Shenzhen Nexus.HTML` 拆分为工程化前端项目（React/Vue + Vite + TypeScript）
- **P0**：建立模块化目录（`explore`、`leaderboard`、`merchant`、`service`、`pass`、`profile`）
- **P0**：搭建基础路由、全局布局、主题与设计 Token
- **P1**：抽离通用组件（卡片、标签、弹层、按钮、Toast、空状态、骨架屏）

### 3.2 Top Spots 融合榜单页

- **P0**：实现分类筛选（`all / landmark / tech / culture / food`）
- **P0**：实现 Spot 动态卡片渲染（热度、标签、场景匹配、附近商户数）
- **P0**：实现榜单状态管理（当前分类、数据加载、异常兜底）
- **P1**：支持按时段/距离/热度切换排序（与后端推荐策略联动）

### 3.3 Spot 融合详情层（Fusion Overlay）

- **P0**：实现融合详情弹层结构（标题、场景说明、消费路径、商户列表）
- **P0**：实现四类 CTA 入口：`Claim Pass`、`Book Service`、`Need Translator`、`Get Directions`
- **P0**：完成 CTA 触发后的页面跳转与状态传递
- **P1**：增加“继续浏览同类 Spot”与“替换商户”交互

### 3.4 商户与交易前台页面

- **P1**：重构 APEC 友好商户列表页与详情页
- **P1**：完成服务预约页（时间段、人数、语言偏好）
- **P1**：完成支付确认页与订单结果页
- **P1**：完成 Pass 卡领取页与核销引导页
- **P2**：接入收藏、历史记录与偏好推荐入口

### 3.5 国际化与无障碍

- **P0**：将 `i18n` 词典拆分为独立资源文件（中英双语闭环）
- **P0**：保证榜单、融合详情层、CTA、错误提示全部双语可用
- **P1**：扩展日语/法语/西语/俄语资源
- **P1**：补齐可访问性（键盘可操作、对比度、读屏标签）

---

### 4. 服务端与业务域任务

### 4.1 API 网关与通用能力

- **P0**：建立统一 API 网关与鉴权中间层
- **P0**：统一响应结构、错误码体系、幂等约束
- **P1**：接入限流、防刷、防重放策略

### 4.2 Spot 与商户业务域

- **P0**：实现 `Spot`、`Merchant` 基础查询接口
- **P0**：实现 `SpotMerchantRelation` 关系查询接口（多对多）
- **P0**：支持按类别、距离、营业状态、标签过滤
- **P1**：支持推荐权重、人工置顶、活动加权等策略参数

### 4.3 融合链路服务

- **P0**：实现榜单接口（分类、排序、分页）
- **P0**：实现融合详情接口（场景匹配、建议路径、关联商户）
- **P0**：实现 CTA 跳转上下文透传（spotId、merchantId、source）
- **P1**：实现商户替补与不可用降级策略（关店/满约/拥挤）

### 4.4 订单、支付与核销域

- **P0**：设计统一订单模型与状态机（待支付/已支付/履约中/已完成/退款）
- **P0**：接入至少一种国际支付方式（如 Stripe）并完成回调验签
- **P1**：实现 Pass 领取、二维码生成、核销与防重复校验
- **P1**：实现退款、异常订单、补单流程
- **P2**：实现佣金分润、商户结算、财务对账报表

### 4.5 AI 与多模态服务

- **P0**：AI 调用改为服务端代理，禁止前端直连模型
- **P0**：实现文本问答、结构化微行程、AR 解码接口
- **P1**：实现实时翻译与 TTS 接口
- **P1**：建立 Prompt 模板与低质量回答回流机制
- **P2**：引入 RAG 与推荐解释能力

---

### 5. 数据模型与埋点任务

### 5.1 核心数据模型

- **P0**：落地 `Spot` 数据模型（类别、热度、标签、文案）
- **P0**：落地 `Merchant` 数据模型（支付能力、语种能力、营业状态）
- **P0**：落地 `SpotMerchantRelation`（距离、匹配分、推荐权重、状态）
- **P0**：落地 `Pass` / `Redemption` 模型
- **P1**：落地 `FusionActionLog` 行为日志模型

### 5.2 融合链路埋点（必须）

- **P0**：实现 `leaderboard_exposure`
- **P0**：实现 `leaderboard_category_click`
- **P0**：实现 `spot_card_click`
- **P0**：实现 `fusion_overlay_open`
- **P0**：实现 `fusion_cta_click_pass` / `fusion_cta_click_booking` / `fusion_cta_click_translate` / `fusion_cta_click_map`
- **P1**：实现 `merchant_click_from_spot` 与 `redeem_success_by_spot` 归因

### 5.3 数据看板与指标

- **P1**：搭建融合链路漏斗看板（曝光→点击→CTA→支付/核销）
- **P1**：按 Spot、商户、时段输出转化报表
- **P2**：接入推荐策略 AB 实验指标

---

### 6. 后台系统任务

### 6.1 运营后台

- **P1**：Spot 内容管理（上下线、排序、分类）
- **P1**：Spot-商户关系管理（绑定、解绑、权重）
- **P1**：活动与 Pass 配置管理
- **P1**：融合链路数据看板
- **P2**：推荐策略配置中心与实验管理

### 6.2 商户后台

- **P1**：商户资料维护与 APEC 标签维护
- **P1**：营业时段、服务能力、优惠权益配置
- **P2**：订单、核销、对账与结算视图

---

### 7. 测试与质量保障任务

### 7.1 自动化测试

- **P0**：搭建单元测试、集成测试、E2E（Playwright）框架
- **P0**：覆盖主闭环 E2E：榜单→详情→CTA→支付/核销
- **P1**：覆盖 AI 问答、微行程、AR 解码、翻译等核心路径

### 7.2 稳定性与性能

- **P1**：移动端适配与主流国际机型浏览器兼容
- **P1**：弱网/高延迟场景测试
- **P1**：高峰压测（APEC 期间并发预估）
- **P2**：静态资源与接口缓存优化

### 7.3 安全与合规

- **P0**：输入输出安全校验、敏感信息扫描
- **P1**：隐私协议、Cookie 同意、多语言合规文案
- **P1**：日志脱敏与数据访问审计

---

### 8. DevOps 与发布任务

### 8.1 CI/CD

- **P0**：建立代码检查、测试、构建、发布流水线
- **P0**：建立前端静态资源发布与回滚机制
- **P0**：建立后端服务发布与灰度策略
- **P1**：建立数据库迁移脚本与版本管理规范

### 8.2 监控告警

- **P0**：接入前端错误监控与 API 性能监控
- **P1**：监控支付成功率、CTA 转化率、核销成功率
- **P1**：建立告警与值班响应机制

### 8.3 环境治理

- **P0**：开发/测试/生产环境隔离与密钥管理
- **P1**：数据库备份恢复演练
- **P1**：高峰容量评估与应急预案

---

### 9. 分阶段排期建议（可直接排 Sprint）

### Phase 1（2~3 周）：工程化 + 融合链路 MVP

- 前端工程拆分、路由与组件基础
- Top Spots 榜单与融合详情层
- 融合链路核心接口与数据模型初版
- 中英双语闭环
- 融合链路核心埋点上线

### Phase 2（2~3 周）：交易闭环 V1

- 商户详情、预约、订单、支付
- Pass 领取与核销
- CTA 到交易链路打通
- 主链路 E2E 自动化测试

### Phase 3（2 周）：后台与优化 V1.1

- 运营后台与商户后台基础功能
- 转化看板与归因分析
- 推荐策略优化与灰度发布
- 性能、安全、监控与上线验收

---

### 10. 模块验收标准（DoD）

### 10.1 Top Spots 融合模块

- **验收条件**：分类筛选、卡片渲染、详情层打开稳定；四类 CTA 全可触发
- **验收数据**：埋点完整上报，`spot_card_click` 与 `fusion_overlay_open` 可追溯

### 10.2 交易与核销模块

- **验收条件**：下单、支付、Pass 领取、核销链路可闭环
- **验收数据**：订单状态机可追踪，核销防重有效

### 10.3 商户承接模块

- **验收条件**：商户标签、能力、营业状态展示正确；Spot-商户关系生效
- **验收数据**：`merchant_click_from_spot` 与 `redeem_success_by_spot` 可归因

### 10.4 质量与发布模块

- **验收条件**：关键 E2E 用例通过，监控告警生效，支持灰度与回滚
- **验收数据**：发布后关键错误率、支付成功率、核销成功率在阈值内

---

### 11. 交付物清单

- 前端工程化代码仓库
- 后端 API 与业务服务代码仓库
- 数据模型设计文档（含 Spot/Merchant/Relation/ActionLog）
- 接口文档与错误码规范
- 埋点方案与看板配置说明
- 自动化测试报告（单测/集成/E2E）
- 上线检查清单与回滚预案
- 运营后台与商户后台使用手册
